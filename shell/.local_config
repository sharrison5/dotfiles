# .local_config
# Sam Harrison 2018
# MIT License: a full version of the license is included in the LICENSE file.

# This contains key config that tends to change from machine-to-machine. The
# variables set here are meant to be available for .profile or .rc files to
# incorporate into the environment as necessary, rather than this file doing
# the setup itself.

# By adding any local variables here, it is more straightforward to merge
# upstream changes. See `.profile` for more information.


#echo ".local_config"


# Locale
export LANG=en_GB.UTF-8

# For shell prompt
export MACHINE_NAME='\h'

export CONDA_DIR=
export CONDA_DEFAULT_ENV=

export FSLDIR=

# For wttr.in
export WTTR_LOCATION=
